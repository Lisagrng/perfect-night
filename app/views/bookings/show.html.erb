<div class="container">
<div class="body">
    <br>
    <h2 class="title"> <%= @booking.activity.name %> ⚡️</h2>
      <br>
      <div class= "image-show">
        <%= cl_image_tag @activity.photo.key%>
      </div>

      <div class="cards">
          <div class="card-booking">
            <p span style="color: #000"><strong>✅ Début</strong></span> de l'activité à : <%=@activity.open_at%></p>
          </div>
          <div class="card-booking">
            <p span style="color: #000"><strong>✅ Fin</strong></span> de l'activité à : <%=@activity.close_at%></p>
          </div>
          <div class="card-booking">
            <p span style="color: #000"><strong>✅ Prix</strong></span> : <%= @price %>
          </div>
      </div>
    <div class="text-center">
        <button class="button-booking">
          <%= link_to "Supprimer cette réservation", booking_path(@booking),
          data: {turbo_method: :delete, turbo_confirm: "Êtes-vous sûre ? 💤"} %>
        </button>
    </div>

    <br>
    <div class="text-center">
        <button class="button-booking">
            <%= link_to 'Voir toutes mes réservations', bookings_path(@booking) %>
        </button>
    </div>
  </div>
</div>
